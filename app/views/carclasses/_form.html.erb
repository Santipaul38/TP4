<%= form_with(model: carclass, local: true) do |form| %>
  <% if carclass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carclass.errors.count, "error") %> prohibited this carclass from being saved:</h2>

      <ul>
        <% carclass.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
